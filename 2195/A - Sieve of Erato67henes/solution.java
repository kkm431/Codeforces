import java.util.*; public class Main {    static boolean canMake67(int[] arr, int index, long product) {        if (product == 67) return true;        if (index == arr.length || product > 67) return false;        // Choose or skip the current element        return canMake67(arr, index + 1, product) || canMake67(arr, index + 1, product * arr[index]);    }     public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt(); // number of test cases        while (t-- > 0) {            int n = sc.nextInt();            int[] arr = new int[n];            for (int i = 0; i < n; i++) arr[i] = sc.nextInt();             System.out.println(canMake67(arr, 0, 1) ? "YES" : "NO");        }        sc.close();    }}