import java.util.Scanner; public class A_Maple_and_Multiplication {    public static void main(String[] args) {        // Create scanner object for input        Scanner sc = new Scanner(System.in);                // Read number of test cases        int t = sc.nextInt();                // Process each test case        while (t-- > 0) {            // Example: Read an integer as input            int a = sc.nextInt();            int b = sc.nextInt();             if (a == b) {                System.out.println(0); // already equal            } else if (a % b == 0 || b % a == 0) {                System.out.println(1); // one can be made equal by multiplying once            } else {                System.out.println(2); // otherwise always 2 operations            }        }                // Close the scanner        sc.close();    }}