import java.util.*; public class B_Cake_Collection {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt(); // number of test cases         while (t-- > 0) {            int n = sc.nextInt();            long m = sc.nextLong();            Integer[] a = new Integer[n];             for (int i = 0; i < n; i++) {                a[i] = sc.nextInt();            }             // Sort in descending order (largest ovens first)            Arrays.sort(a, Collections.reverseOrder());             long ans = 0;                         for (int i = 0; i < n && m >=0 ; i++) {                 ans += (long) a[i] * m;                m--;            }             System.out.println(ans);        }         sc.close();    }}