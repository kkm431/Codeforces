import java.util.Scanner; public class B_Worms {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);                int n = sc.nextInt();        int[] a= new int[n];         for (int i = 0; i < a.length; i++) {            a[i] = sc.nextInt();        }                 long[][] piles= new long[n][2];                 // pile = (1,2; 3,7+2; 9+1, 12; 13, 16; 17, 25)        for (int i = 0; i < n; i++) {            if(i ==0){                piles[i][0] = 1;                piles[i][1] = a[i];            }            else{                piles[i][0] = piles[i-1][1] + 1;                piles[i][1] = piles[i-1][1] + a[i];            }        }         int m = sc.nextInt();                        for (int i = 0; i < m; i++) {            int juice = sc.nextInt();            int res = binarySearch(piles, juice, n);            System.out.println(res);        }          //Binary search to find piles                  sc.close();    }     public static int binarySearch(long[][] piles, int num, int length) {        int left = 0, right = length - 1;            while (left <= right) {            int mid = left + (right - left) / 2;             if (piles[mid][0] <= num && num <= piles[mid][1]) {                return mid + 1;             } else if (num < piles[mid][0]) {                right = mid - 1;             } else {                left = mid + 1; // Search left half            }        }            // If num is not found, `left` is the correct insertion index        return -1;    }}