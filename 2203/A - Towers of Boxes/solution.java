import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);                int t = sc.nextInt();  // number of test cases                while (t-- > 0) {            int n = sc.nextInt();            int m = sc.nextInt();            int d = sc.nextInt();                        // Maximum boxes per tower            int maxPerTower = (d / m) + 1;                        // Minimum towers required (ceiling division)            int towers = (n + maxPerTower - 1) / maxPerTower;                        System.out.println(towers);        }                sc.close();    }}