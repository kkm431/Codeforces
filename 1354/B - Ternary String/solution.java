import java.util.Scanner; public class B_Ternary_String {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);                int t = sc.nextInt();                while (t-- > 0) {            String str = sc.next();            int n= str.length();            int[] ctr = new int[4];                        //two pointer left , right -> extend right till all 3 in between left and right index....            // -> now shrink left to remove unwanted. meanwhile track minimum length of window when all 3 present               int left = 0, right =0;            int distinct = 0;             int minLen = Integer.MAX_VALUE;             while(right < n){                int num = str.charAt(right) - '0';                if(ctr[num] == 0){                    distinct++;                }                ctr[num]++;                 while(distinct == 3){                    minLen= Math.min(minLen, right - left + 1);                                    //Shrnk left                    int leftNum = str.charAt(left) - '0';                    left++;                    ctr[leftNum]--;                    if(ctr[leftNum] == 0){                        distinct--;                    }                 }                                right++;            }             System.out.println((minLen == Integer.MAX_VALUE) ? 0 : minLen);         }                sc.close();    }}